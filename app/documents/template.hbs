<ul class="list-unstyled">
    {{#each model as |document|}}
        {{document-history
            document=document
            collapseVersions=true
            updateComparison=(action 'updateComparison')
        }}
    {{/each}}
</ul>
